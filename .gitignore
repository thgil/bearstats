# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Raw data dumps (too big for git, reproducible from scripts)
data-pipeline/raw/

# Webapp build artifacts (none yet, but defensive)
webapp/node_modules/

# macOS / editor
.DS_Store
.vscode/
.idea/

# wrangler local cache
.wrangler/

# Render pipeline (workstream D): deps and intermediate PNG frames are
# regenerated by tools/render-video.mjs; only the encoded MP4s are committed.
tools/node_modules/
media/frames/

# tools/review-shots.mjs output: screenshots + report.json, regenerated on
# every run, never committed.
media/review/
